<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Report an Incident - Disaster Management Portal</title>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="stylesheet" href="/css/chatbot.css" />

  <style>
    /* ===== Base ===== */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      min-height: 100vh;
      color: #0f172a;
      background: #f3faf6;
    }

    /* ===== Background Animation ===== */
    .bg-animation {
      position: fixed;
      inset: 0;
      overflow: hidden;
      z-index: -1;
      background: radial-gradient(circle at top left, #e0f7ec, #f8fffb 55%);
    }

    .bg-animation span {
      position: absolute;
      display: block;
      width: 210px;
      height: 210px;
      background: rgba(15, 157, 88, 0.18);
      border-radius: 50%;
      filter: blur(22px);
      animation: float 18s ease-in-out infinite;
    }

    .bg-animation span:nth-child(1) { top: 6%; left: 4%; animation-delay: -2s; }
    .bg-animation span:nth-child(2) { top: 40%; left: 78%; animation-delay: -5s; }
    .bg-animation span:nth-child(3) { top: 72%; left: 10%; animation-delay: -8s; }
    .bg-animation span:nth-child(4) { top: 20%; left: 55%; animation-delay: -11s; }
    .bg-animation span:nth-child(5) { top: 86%; left: 72%; animation-delay: -14s; }

    @keyframes float {
      0%   { transform: translateY(0) scale(1);   opacity: 0.45; }
      50%  { transform: translateY(-60px) scale(1.15); opacity: 0.8; }
      100% { transform: translateY(0) scale(1);   opacity: 0.45; }
    }

    /* ===== Navbar ===== */
    header {
      position: sticky;
      top: 0;
      z-index: 10;
    }

    header nav {
      background: #0f9d58;
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: white;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.18);
      backdrop-filter: blur(10px);
    }

    .dash-logo {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      color: inherit;
    }

    .dash-logo img {
      height: 42px;
      width: auto;
      border-radius: 10px;
      box-shadow: 0 0 12px rgba(15, 157, 88, 0.6);
    }

    .dash-logo span {
      font-weight: 700;
      font-size: 1.05rem;
      letter-spacing: 0.04em;
      
    }

    .home-btn {
      background: #ffffff;
      color: #0f9d58;
      padding: 6px 14px;
      border-radius: 999px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.88rem;
      border: 1px solid transparent;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: background 0.2s, transform 0.1s, box-shadow 0.2s, border-color 0.2s;
    }

    .home-btn:hover {
      background: #e8f8ef;
      border-color: rgba(15, 157, 88, 0.5);
      box-shadow: 0 0 10px rgba(15, 157, 88, 0.3);
      transform: translateY(-1px);
    }

    /* ===== Layout ===== */
    main {
      max-width: 960px;
      margin: 2.4rem auto 3.4rem;
      padding: 0 1.2rem;
    }

    /* ===== Card ===== */
    .section-card {
      background: rgba(255, 255, 255, 0.95);
      padding: 1.9rem 1.8rem 2.1rem;
      border-radius: 1.2rem;
      box-shadow: 0 18px 40px rgba(15, 157, 88, 0.18);
      border: 1px solid rgba(148, 163, 184, 0.28);
      position: relative;
      overflow: hidden;
    }

    .section-card::before {
      content: "";
      position: absolute;
      width: 140px;
      height: 140px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(15, 157, 88, 0.22), transparent 60%);
      top: -50px;
      right: -30px;
      opacity: 0.6;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      margin-bottom: 0.5rem;
      position: relative;
      z-index: 1;
    }

    .section-header i {
      color: #0f9d58;
      font-size: 1.3rem;
    }

    .section-header h2 {
      font-size: 1.5rem;
      color: #064e3b;
    }

    #report > .section-card > p {
      margin-top: 0.2rem;
      margin-bottom: 1.2rem;
      font-size: 0.95rem;
      color: #4b5563;
    }

    /* ===== Form Grid ===== */
    .report-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem 1.2rem;
      margin-top: 0.5rem;
      position: relative;
      z-index: 1;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      font-size: 0.9rem;
    }

    .form-group-full {
      grid-column: span 2;
    }

    label {
      font-weight: 600;
      color: #111827;
    }

    label span {
      color: #dc2626;
      margin-left: 2px;
    }

    input,
    select,
    textarea {
      width: 100%;
      padding: 0.55rem 0.7rem;
      border-radius: 0.6rem;
      border: 1px solid #cbd5e1;
      font-size: 0.9rem;
      outline: none;
      background: #f9fafb;
      transition: border-color 0.2s, box-shadow 0.2s, background 0.2s, transform 0.05s;
    }

    textarea {
      min-height: 110px;
      resize: vertical;
    }

    input:focus,
    select:focus,
    textarea:focus {
      border-color: #0f9d58;
      background: #ffffff;
      box-shadow: 0 0 0 1px rgba(15, 157, 88, 0.3);
      transform: translateY(-1px);
    }

    /* ===== Footer row in form ===== */
    .form-footer {
      margin-top: 1.1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 0.8rem;
      flex-wrap: wrap;
      font-size: 0.8rem;
      color: #6b7280;
      position: relative;
      z-index: 1;
    }

    .required-star {
      color: #dc2626;
      margin-right: 2px;
    }

    .btn-primary {
      background: linear-gradient(135deg, #0f9d58, #16a34a);
      color: white;
      padding: 0.55rem 1.4rem;
      border: none;
      border-radius: 999px;
      font-size: 0.9rem;
      cursor: pointer;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 0.45rem;
      box-shadow: 0 8px 20px rgba(15, 157, 88, 0.5);
      transition: transform 0.12s ease-out, box-shadow 0.12s ease-out, filter 0.12s;
    }

    .btn-primary i {
      font-size: 0.9rem;
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 12px 26px rgba(15, 157, 88, 0.7);
      filter: brightness(1.03);
    }

    .btn-primary:active {
      transform: translateY(0);
      box-shadow: 0 6px 14px rgba(15, 157, 88, 0.6);
    }

    /* ===== Status Message ===== */
    .status-msg {
      margin-top: 0.8rem;
      font-size: 0.9rem;
      font-weight: 600;
      min-height: 1.2rem;
      transition: 0.25s ease;
    }

    .status-success {
      color: #15803d;
    }

    .status-error {
      color: #b91c1c;
    }

    .status-wait {
      color: #b45309;
    }

    /* Small badge for hint */
    .hint-badge {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 0.78rem;
      padding: 0.2rem 0.55rem;
      border-radius: 999px;
      background: #ecfdf3;
      color: #15803d;
      margin-bottom: 0.55rem;
    }

    .hint-badge i {
      font-size: 0.8rem;
    }

    /* ===== Success Modal ===== */
    .modal-overlay {
      position: fixed;
      inset: 0;
      background: rgba(15, 23, 42, 0.55);
      display: none;
      align-items: center;
      justify-content: center;
      z-index: 30;
    }

    .modal-overlay.show {
      display: flex;
    }

    .modal-content {
      background: #ffffff;
      border-radius: 1rem;
      padding: 1.6rem 1.8rem 1.4rem;
      max-width: 360px;
      width: 90%;
      text-align: center;
      box-shadow: 0 18px 45px rgba(15, 157, 88, 0.45);
      position: relative;
    }

    .modal-icon {
      width: 54px;
      height: 54px;
      border-radius: 999px;
      margin: 0 auto 0.6rem;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #ecfdf3;
      color: #16a34a;
      font-size: 1.5rem;
      box-shadow: 0 0 0 6px rgba(22, 163, 74, 0.12);
    }

    .modal-content h3 {
      font-size: 1.2rem;
      margin-bottom: 0.4rem;
      color: #065f46;
    }

    .modal-content p {
      font-size: 0.9rem;
      color: #4b5563;
      margin-bottom: 0.9rem;
    }

    .modal-actions {
      display: flex;
      justify-content: center;
      gap: 0.6rem;
      flex-wrap: wrap;
    }

    .modal-btn {
      border-radius: 999px;
      padding: 0.45rem 1.1rem;
      border: none;
      cursor: pointer;
      font-size: 0.85rem;
      font-weight: 600;
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
    }

    .modal-btn-primary {
      background: #0f9d58;
      color: white;
      box-shadow: 0 6px 16px rgba(15, 157, 88, 0.6);
    }

    .modal-btn-secondary {
      background: #e5e7eb;
      color: #111827;
    }

    .modal-btn-primary:hover {
      filter: brightness(1.05);
      transform: translateY(-1px);
    }

    .modal-btn-secondary:hover {
      background: #d1d5db;
    }

    .modal-close-x {
      position: absolute;
      top: 8px;
      right: 10px;
      font-size: 1.1rem;
      color: #9ca3af;
      cursor: pointer;
    }

    .modal-close-x:hover {
      color: #4b5563;
    }

    /* ===== Back to Top Button ===== */
    .back-to-top {
      position: fixed;
      right: 14px;
      bottom: 16px;
      width: 40px;
      height: 40px;
      border-radius: 999px;
      border: none;
      background: #0f9d58;
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      box-shadow: 0 8px 18px rgba(15, 157, 88, 0.6);
      opacity: 0;
      pointer-events: none;
      transform: translateY(10px);
      transition: opacity 0.2s ease, transform 0.2s ease;
      z-index: 20;
    }

    .back-to-top.show {
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }

    .back-to-top i {
      font-size: 1rem;
    }

    .back-to-top:hover {
      filter: brightness(1.05);
      box-shadow: 0 10px 22px rgba(15, 157, 88, 0.7);
    }

    @media (max-width: 768px) {
      main {
        margin-top: 1.8rem;
      }

      .section-card {
        padding: 1.4rem 1.2rem 1.7rem;
      }

      .report-grid {
        grid-template-columns: 1fr;
      }

      .form-group-full {
        grid-column: span 1;
      }

      header nav {
        flex-direction: row;
        gap: 0.75rem;
      }

      .dash-logo span {
        font-size: 0.9rem;
      }

      .home-btn {
        padding: 6px 10px;
        font-size: 0.8rem;
      }
    }
  </style>
</head>
<body>

  <!-- Background Animation -->
  <div class="bg-animation">
    <span></span><span></span><span></span><span></span><span></span>
  </div>

  <!-- Navbar -->
  <header>
    <nav>
      <a href="/" class="dash-logo">
        <img src="/assets/logo.png" alt="Disaster Hub Logo" />
        <span>Disaster Learning Hub</span>
      </a>

      <a href="/" class="home-btn">
        <i class="fa fa-home"></i>
        Home
      </a>
    </nav>
  </header>

  <!-- Main Report Form -->
  <main>
    <section id="report">
      <div class="section-card report-card">
        <div class="section-header">
          <i class="fa-solid fa-bullhorn"></i>
          <h2>Report an Incident</h2>
        </div>

        <p>
          If you witness or are affected by a disaster, please report it immediately.
          Your information will help authorities respond faster and more accurately.
        </p>

        <span class="hint-badge">
          <i class="fa-regular fa-lightbulb"></i>
          Tip: Keep details clear but avoid sharing private info.
        </span>

        <form id="reportForm">
          <div class="report-grid">
            <div class="form-group">
              <label for="name">Your Name <span>*</span></label>
              <input
                type="text"
                id="name"
                placeholder="Enter your full name"
                name="report[name]"
                required
              />
            </div>

            <div class="form-group">
              <label for="phone">Contact Number <span>*</span></label>
              <input
                type="tel"
                id="phone"
                placeholder="e.g., +91 98765 43210"
                name="report[phone]"
                required
              />
            </div>

            <div class="form-group">
              <label for="type">Type of Disaster <span>*</span></label>
              <select id="type" required name="report[disaster]">
                <option value="">Select type</option>
                <option>Earthquake</option>
                <option>Flood</option>
                <option>Fire</option>
                <option>Landslide</option>
                <option>Industrial Accident</option>
                <option>Other</option>
              </select>
            </div>

            <div class="form-group">
              <label for="location">Location <span>*</span></label>
              <input
                type="text"
                id="location"
                placeholder="City / Area / Landmark"
                name="report[location]"
                required
              />
            </div>

            <div class="form-group form-group-full">
              <label for="details">Incident Details <span>*</span></label>
              <textarea
                id="details"
                placeholder="Describe what happened, time, people affected, visible damage..."
                required
              ></textarea>
            </div>
          </div>

          <div class="form-footer">
            <span>
              <span class="required-star">*</span> All required fields must be filled.
              Do not share sensitive personal details of others.
            </span>

            <button type="submit" class="btn-primary">
              <i class="fa-solid fa-paper-plane"></i>
              Submit Report
            </button>
          </div>

          <div id="statusMsg" class="status-msg"></div>
        </form>
      </div>
    </section>
  </main>

  <!-- Success Modal -->
  <div id="successModal" class="modal-overlay">
    <div class="modal-content">
      <span class="modal-close-x" id="modalCloseX">&times;</span>
      <div class="modal-icon">
        <i class="fa-solid fa-check"></i>
      </div>
      <h3>Report Submitted</h3>
      <p>
        Thank you for reporting this incident. Our disaster response team has been notified
        and will act based on the information you provided.
      </p>
      <div class="modal-actions">
        <button class="modal-btn modal-btn-primary" id="modalOkBtn">
          <i class="fa-solid fa-thumbs-up"></i> OK
        </button>
        <button class="modal-btn modal-btn-secondary" id="modalHomeBtn">
          <i class="fa fa-home"></i> Go to Home
        </button>
      </div>
    </div>
  </div>

  <!-- Back to Top Button -->
  <button id="backToTop" class="back-to-top">
    <i class="fa-solid fa-arrow-up"></i>
  </button>

  <!-- JS for form, modal & back-to-top -->
  <script>
    const reportForm = document.getElementById("reportForm");
    const statusMsg = document.getElementById("statusMsg");
    const successModal = document.getElementById("successModal");
    const modalCloseX = document.getElementById("modalCloseX");
    const modalOkBtn = document.getElementById("modalOkBtn");
    const modalHomeBtn = document.getElementById("modalHomeBtn");
    const backToTopBtn = document.getElementById("backToTop");

    function openModal() {
      successModal.classList.add("show");
    }

    function closeModal() {
      successModal.classList.remove("show");
    }

    reportForm.addEventListener("submit", function (e) {
      e.preventDefault();

      const name = document.getElementById("name").value.trim();
      const phone = document.getElementById("phone").value.trim();
      const type = document.getElementById("type").value.trim();
      const location = document.getElementById("location").value.trim();
      const details = document.getElementById("details").value.trim();

      if (!name || !phone || !type || !location || !details) {
        statusMsg.textContent = "⚠ Please fill all required fields.";
        statusMsg.className = "status-msg status-error";
        return;
      }

      statusMsg.textContent = "⏳ Submitting incident report...";
      statusMsg.className = "status-msg status-wait";

      setTimeout(() => {
        statusMsg.textContent =
          "✅ Report submitted successfully! Opening confirmation...";
        statusMsg.className = "status-msg status-success";
        reportForm.reset();
        openModal();
      }, 1200);
    });

    // Modal button events
    modalCloseX.addEventListener("click", closeModal);
    modalOkBtn.addEventListener("click", closeModal);
    modalHomeBtn.addEventListener("click", function () {
      window.location.href = "/";
    });

    // Close modal if clicking outside content
    successModal.addEventListener("click", function (e) {
      if (e.target === successModal) {
        closeModal();
      }
    });

    // Back to Top: show/hide based on scroll
    window.addEventListener("scroll", function () {
      if (window.scrollY > 200) {
        backToTopBtn.classList.add("show");
      } else {
        backToTopBtn.classList.remove("show");
      }
    });

    // Smooth scroll to top
    backToTopBtn.addEventListener("click", function () {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  </script>
  
  <!-- Chatbot Widget -->
  <%- include('chatbot-widget') %>
  
  <!-- Chatbot Script -->
  <script src="/js/chatbot.js"></script>
</body>
</html>


